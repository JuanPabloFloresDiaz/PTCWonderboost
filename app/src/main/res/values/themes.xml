<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PTCWonderboost" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorAccent">@color/darkblue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/yellow</item>
        <item name="colorSecondaryVariant">@color/your_rgb_color</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorAccent</item>
        <item name="android:textColor">?attr/colorPrimary</item>

        <!-- Customize your theme here. -->
    </style>

    <style name="BottomNavigationViewStyle">
        <item name="android:background">@color/yellow</item>
        <item name="itemIconTint">@color/darkblue</item>
        <item name="itemTextColor">@color/darkblue</item>
    </style>
    <style name="Theme.PTCWonderboost" parent="Base.Theme.PTCWonderboost" />

    <style name="NavigationViewStyle">
        <!-- Cambia el color del texto de los elementos del menú -->
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@color/yellow</item>
        <!-- Cambia el color de los íconos de los elementos del menú -->
        <item name="itemIconTint">@color/black</item>
    </style>

</resources>